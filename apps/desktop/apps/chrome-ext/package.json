{
  "name": "@gedfix/chrome-ext",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "zip": "cd dist && zip -r ../gedfix-chrome-ext.zip ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "@types/chrome": "^0.1.40"
  },
  "dependencies": {
    "sql.js": "^1.14.1"
  }
}
